# Copy to `.dev.vars` (gitignored) for local `wrangler pages dev` runs.
# Production values are set in the Cloudflare Pages dashboard, not here.
ANTHROPIC_API_KEY=sk-ant-...
# Optional: enable the n8n logging / gap-alert side-channel locally.
# N8N_WEBHOOK_URL=https://your-n8n-host/webhook/kb-chat
